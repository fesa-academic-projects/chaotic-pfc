# ─────────────────────────────────────────────────────────────────────────
# requirements-lock.txt
# ─────────────────────────────────────────────────────────────────────────
# Exact versions against which the CI currently runs green on Python 3.11–3.14.
# Regenerate with:
#
#   python -m venv /tmp/lock-venv
#   /tmp/lock-venv/bin/pip install -e ".[dev,fast]"
#   /tmp/lock-venv/bin/pip freeze --exclude-editable > requirements-lock.txt
#
# Install with:
#
#   pip install -r requirements-lock.txt
#
# For day-to-day development use `pip install -e ".[dev]"` instead — that
# follows the loose version ranges declared in pyproject.toml and picks
# up security fixes automatically. The lock file is intended for
# reproducing the exact environment of a given CI run (e.g. when
# debugging a regression).
# ─────────────────────────────────────────────────────────────────────────

cfgv==3.5.0
contourpy==1.3.3
coverage==7.14.1
cycler==0.12.1
distlib==0.4.3
filelock==3.29.4
fonttools==4.63.0
identify==2.6.19
iniconfig==2.3.0
kiwisolver==1.5.0
llvmlite==0.47.0
matplotlib==3.11.0
mypy==2.1.0
mypy_extensions==1.1.0
nodeenv==1.10.0
numba==0.65.1
numpy==2.4.6
packaging==26.2
pathspec==1.1.1
pillow==12.2.0
platformdirs==4.10.0
pluggy==1.6.0
pre-commit==4.6.0
pygments==2.20.0
pyparsing==3.3.2
pytest==9.1.0
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
pyyaml==6.0.3
ruff==0.15.17
scipy==1.17.1
six==1.17.0
typing_extensions==4.15.0
virtualenv==21.5.1
