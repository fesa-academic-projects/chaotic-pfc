# ─────────────────────────────────────────────────────────────────────────
# requirements-lock.txt
# ─────────────────────────────────────────────────────────────────────────
# Exact versions against which the CI currently runs green on Python 3.12.
# Regenerate with:
#
#   python -m venv /tmp/lock-venv
#   /tmp/lock-venv/bin/pip install -e ".[dev]"
#   /tmp/lock-venv/bin/pip freeze --exclude-editable > requirements-lock.txt
#
# Install with:
#
#   pip install -r requirements-lock.txt
#
# For day-to-day development use `pip install -e ".[dev]"` instead — that
# follows the loose version ranges declared in pyproject.toml and picks
# up security fixes automatically. The lock file is intended for
# reproducing the exact environment of a given CI run (e.g. when
# debugging a regression).
# ─────────────────────────────────────────────────────────────────────────

cfgv==3.5.0
contourpy==1.3.3
coverage==7.13.5
cycler==0.12.1
distlib==0.4.0
filelock==3.29.0
fonttools==4.62.1
identify==2.6.19
iniconfig==2.3.0
kiwisolver==1.5.0
llvmlite==0.47.0
matplotlib==3.10.9
mypy==1.20.2
mypy_extensions==1.1.0
nodeenv==1.10.0
numba==0.65.0
numpy==2.4.4
packaging==26.1
pathspec==1.1.0
pillow==12.2.0
platformdirs==4.9.6
pluggy==1.6.0
pre-commit==4.6.0
pygments==2.20.0
pyparsing==3.3.2
pytest==9.0.3
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
pyyaml==6.0.3
ruff==0.15.11
scipy==1.17.1
six==1.17.0
typing_extensions==4.15.0
virtualenv==21.2.4
